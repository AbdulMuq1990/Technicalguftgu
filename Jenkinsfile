pipeline {
    agent any
    
    stages {
        stage('Git Checkout') {
            steps {
                sh 'git clone https://github.com/AbdulMuq1990/Technicalguftgu.git'
            }
        }
    
        stage('Webhook') {
            steps {
                sh 'ls -l'
            }
        }
     }
  }
